---
description: 象棋卜卦 LIFF 專案的核心開發標準和文檔管理規範
alwaysApply: true
---

# 專案核心標準

## 📋 文檔管理（必須遵守）

**規則**：
- **請優先更新既有的文檔，如需添加額外文檔前須要先詢問才能新增**
- 所有文檔更新必須記錄在 `docs/WORKLOG.md` 中
- 保持文檔的繁體中文撰寫

**範例**：
```markdown
❌ BAD - 直接創建新文檔
創建 docs/NEW_FEATURE.md...

✅ GOOD - 先詢問再創建
"我注意到需要記錄新功能，是否應該更新現有的 docs/DESIGN.md，
還是需要建立新文檔？"
```

## 💻 程式碼風格

**JavaScript/Node.js 標準**：
- 使用 ES6+ 語法（async/await、箭頭函數、解構等）
- 函數和變數使用有意義的英文命名（camelCase）
- 註解使用繁體中文，說明業務邏輯
- 錯誤訊息面向用戶時使用繁體中文

**範例**：
```javascript
// ❌ BAD
const a = async () => {
  const d = await getData();
  return d;
};

// ✅ GOOD
/**
 * 從 n8n 獲取棋盤資料
 * @param {string} taskId - 任務ID
 * @returns {Promise<Object>} 棋盤資料
 */
const getBoardData = async (taskId) => {
  const boardData = await n8nClient.getBoardByTaskId(taskId);
  return boardData;
};
```

## 📝 Git 提交規範

**格式**：`[類型] 簡短描述`

**類型**：
- `新增` - 新功能或新文件
- `修改` - 既有功能的變更
- `修復` - Bug 修復
- `重構` - 代碼重構，不改變功能
- `文檔` - 文檔更新

**範例**：
```bash
✅ GOOD
git commit -m "[新增] 實作圖片生成服務"
git commit -m "[修復] 修正數字驗證邏輯錯誤"
git commit -m "[文檔] 更新 API 文檔"

❌ BAD
git commit -m "update"
git commit -m "fix bug"
```

## 🔍 Code Review 檢查清單

每次提交前自我檢查：
- [ ] 是否更新了 WORKLOG？
- [ ] 錯誤處理是否完整？
- [ ] 是否包含必要的註解？
- [ ] 環境變數是否正確配置？
- [ ] 是否測試過邊界情況？
- [ ] 用戶體驗是否友善？
- [ ] 是否符合專案架構規範？
